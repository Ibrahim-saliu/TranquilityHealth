/**
 * lib/auth/index.ts — Barrel export for authentication utilities.
 *
 * Import auth utilities from this module:
 *   import { isAuthenticated, hasRole, requireAuth, requireRole } from "@/lib/auth";
 */

export * from "./middleware";
