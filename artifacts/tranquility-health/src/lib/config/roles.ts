// Role configuration — re-exports the role types and labels used across the app.

import { ROLES, ROLE_LABELS, type UserRole } from "@/types/roles";

export { ROLES, ROLE_LABELS };
export type { UserRole };
