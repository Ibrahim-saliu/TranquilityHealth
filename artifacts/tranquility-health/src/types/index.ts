/**
 * types/index.ts — Barrel export for all type definitions.
 *
 * Import shared types from this file to avoid deep import paths:
 *   import { UserRole, ROLES } from "@/types";
 */

export * from "./roles";
